# دليل نشر مونوبولي بالبلد

المشروع جاهز للنشر عبر Docker على أي منصة بتدعمه (Railway، Render،
Fly.io...). هون خطوات Railway بالتفصيل (الأسهل للبداية)، وRender كبديل.

## قبل ما تبلش

- ✅ ادفع (push) كل المشروع لريبو GitHub خاص فيك (لازم يكون فيه
  `Dockerfile` و`pom.xml` بالجذر، متل ما هو الوضع هلق).
- ✅ غيّر `admin.key` بملف `src/main/resources/application.properties`
  - أو بلاش تعتمد على الملف، وحط القيمة كمتغير بيئة `ADMIN_KEY` من
  لوحة المنصة نفسها (أأمن، مذكور بالخطوات تحت).

## Railway (يدعم Docker وWebSocket تلقائيًا)

1. روح لـ [railway.app](https://railway.app) وسجل دخول بحساب GitHub.
2. **New Project** → **Deploy from GitHub repo** → اختار ريبو المشروع.
3. Railway بيكتشف `Dockerfile` تلقائيًا ويبلش يبنيه (تقدر تتابع التقدم
   بتبويب **Deployments**).
4. من تبويب **Variables**، ضيف:
   - `ADMIN_KEY` = مفتاح قوي من اختيارك (مش "change-me-please").
5. من تبويب **Settings** → **Networking** → **Generate Domain**، حتى
   تاخد رابط عام (شكله تقريبًا `xxx.up.railway.app`).
6. افتح الرابط من موبايلك أو متصفحك — المفروض اللعبة تشتغل عليه مباشرة.

## Render (بديل، نفس الفكرة تقريبًا)

1. [render.com](https://render.com) → **New** → **Web Service**.
2. اربط ريبو GitHub، Render بيكتشف `Dockerfile` تلقائيًا.
3. من **Environment**، ضيف `ADMIN_KEY` بنفس الطريقة.
4. Render بيعطيك رابط عام تلقائيًا بعد أول نشر ناجح.

## ملاحظات مهمة قبل ما تشارك الرابط مع أصحابك

- **كل شي بالذاكرة**: أي إعادة تشغيل أو نشر جديد (redeploy) بيصفّر كل
  الغرف والألعاب الشغالة وبيمسح ملف التقييمات `data/ratings.jsonl`
  كمان، إلا إذا فعّلت "Volume" دائم من إعدادات المنصة (Railway وRender
  الاثنين فيهم هالخيار لو حبيت تحتفظ بالتقييمات بين عمليات النشر).
- **الأداء بالخطط المجانية**: بعض المنصات بتنيّم (sleep) السيرفر بعد
  فترة خمول، فأول طلب بعد فترة سكوت ممكن ياخد كم ثانية زيادة لحد
  ما يصحى — طبيعي، مش عطل.
- تأكد إنك دايمًا تستعمل نفس الرابط النهائي (مش localhost) لما تبعته
  لأصحابك، وتأكد كل واحد فاتح الموقع بالوضع الأفقي على موبايله.
